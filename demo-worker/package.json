{
  "name": "juggernaut-router-track1-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "dev": "wrangler dev --local --port 8787",
    "dry-run": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy --minify"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
